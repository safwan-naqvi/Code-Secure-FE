const Button = ({ styles }) => (
  <button
    type="button"
    className={`${styles} bg-blue-gradient py-4 px-6 font-poppins font-medium text-[18px] rounded-md text-primary outline-none`}
  >
    Get Started
  </button>
);

export default Button;
